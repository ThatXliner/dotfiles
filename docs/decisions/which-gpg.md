# Why `brew install gpg-suite` over GnuPG?

`gpg-suite` (https://gpgtools.org) is the easiest way to setup GPG on macOS, with a single command and no extra configuration. I also haven't tried straight GnuPG on Mac yet, and honestly I don't want to find out how to do it: what works works (don't touch it lol).
